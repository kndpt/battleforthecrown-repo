# maint-debt — 2026-06-15

## Fix: add unit spec for shared resource production lookup helpers

`packages/shared/src/resources/production.ts` exposes `isResourceBuildingType`, `getResourceBuildingProduction`, and `getBuildingProduction` — pure table lookups used by `calculateProductionRate` and Pixi building bonus previews. Only the `RESOURCE_PRODUCTION_PER_HOUR` constant had indirect coverage via `buildings.spec.ts`; the lookup helpers had zero direct tests.

**Change:** new `battleforthecrown-backend/src/modules/resources/resource-production.spec.ts` — 6 tests (type guard, per-minute table lookup, unknown level null, non-resource guard).

**Verification:** `yarn static-check` ✓, `yarn workspace battleforthecrown-backend test resource-production.spec` — 6 tests ✓.

## Backlog updates

- Removed resource-production lookup helpers from implicit debt (fixed).
- Added `combat report view helpers` as new candidate (formatCoord/shortReportId duplication).
- Retained blocked/open-PR items unchanged.
