# maint-debt — 2026-06-15

## Fix: add unit spec for `getStrategyDefinition`

`getStrategyDefinition` in `packages/shared/src/village/strategy.ts` resolves strategy metadata (display name, description, raw bonus overrides) and falls back to BALANCED for unknown runtime keys. It is consumed by `getStrategyBonuses` on the combat/production/building hot path but had no direct unit coverage (PR #111 covers `getStrategyBonusValue` only).

**Change:** new `battleforthecrown-backend/src/modules/strategy/strategy-definition.spec.ts` — 3 tests (FORTRESS metadata, BALANCED empty bonuses, unknown-key fallback).

**Verification:** `yarn static-check` ✓, `yarn workspace battleforthecrown-backend test strategy-definition.spec` — 3/3 ✓.

## Backlog updates

- Skipped candidates already in flight: WorldMapScene (#89), training-time (#99–#101, #110), production-rate (#102), ReportDetailModal console.error (#116), army formatDuration (#114), clampBuildingLevel (#112), quarter population (#104), getVillageStrategyChangeCost (#115), getStrategyBonusValue (#111).
- Added `isFoggedEntity` and `getBuildingProduction` as new candidates.
