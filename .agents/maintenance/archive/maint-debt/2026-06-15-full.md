# maint-debt — 2026-06-15

## Fix: add unit spec for `getVillageStrategyChangeCost`

`getVillageStrategyChangeCost` in `packages/shared/src/village/strategy.ts` scales strategy-change resource costs by castle level (reference level 4, multiplier 1.25 per level above). Used by backend `village-strategy.service` and pixi `VillageStyleControl`, but had no dedicated regression test.

**Change:** added `battleforthecrown-backend/src/modules/strategy/village-strategy-cost.spec.ts` covering base cost at reference level, floor below reference, multiplier scaling, and per-strategy base cost differences.

**Verification:** `yarn static-check` ✓, `jest village-strategy-cost.spec.ts` ✓ (4 tests).

## Backlog updates

- Removed `getVillageStrategyChangeCost` (fixed).
- Marked clampBuildingLevel (#112) and normalizeTier (#108) as open-PR blocked.
