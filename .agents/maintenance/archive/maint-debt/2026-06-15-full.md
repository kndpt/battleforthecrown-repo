# maint-debt — 2026-06-15

## Fix: add unit spec for `getVillageStrategyChangeCost`

`getVillageStrategyChangeCost` in `packages/shared/src/village/strategy.ts` scales strategy-change resource costs by castle level (reference level 4, multiplier 1.25 per level above). Used by backend `village-strategy.service` and pixi `VillageStyleControl`, but had no dedicated regression test.

**Change:** added `battleforthecrown-backend/src/modules/strategy/village-strategy-cost.spec.ts` covering base cost at reference level, floor below reference, multiplier scaling, and per-strategy base cost differences.

**Verification:** `yarn static-check` ✓, `jest village-strategy-cost.spec.ts` ✓ (4 tests).

## Fix: add unit spec for `getStrategyDefinition`

`getStrategyDefinition` in `packages/shared/src/village/strategy.ts` resolves strategy metadata (display name, description, raw bonus overrides) and falls back to BALANCED for unknown runtime keys. It is consumed by `getStrategyBonuses` on the combat/production/building hot path but had no direct unit coverage (PR #111 covers `getStrategyBonusValue` only).

**Change:** new `battleforthecrown-backend/src/modules/strategy/strategy-definition.spec.ts` — 3 tests (FORTRESS metadata, BALANCED empty bonuses, unknown-key fallback).

**Verification:** `yarn static-check` ✓, `yarn workspace battleforthecrown-backend test strategy-definition.spec` — 3/3 ✓.

## Fix: add unit spec for shared resource production lookup helpers

`packages/shared/src/resources/production.ts` exposes `isResourceBuildingType`, `getResourceBuildingProduction`, and `getBuildingProduction` — pure table lookups used by `calculateProductionRate` and Pixi building bonus previews. Only the `RESOURCE_PRODUCTION_PER_HOUR` constant had indirect coverage via `buildings.spec.ts`; the lookup helpers had zero direct tests.

**Change:** new `battleforthecrown-backend/src/modules/resources/resource-production.spec.ts` — 6 tests (type guard, per-minute table lookup, unknown level null, non-resource guard).

**Verification:** `yarn static-check` ✓, `yarn workspace battleforthecrown-backend test resource-production.spec` — 6 tests ✓.

## Fix: add unit spec for `calculateTrainingTime`

`calculateTrainingTime` is on the recruit/noble hot path but had no direct pure-logic spec.

**Change:** new `battleforthecrown-backend/src/modules/gameplay/training-time.spec.ts` — 5 tests (ms conversion, speed multiplier, trainingSpeedBonus, 1s floor, rounding).

**Verification:** `yarn static-check` ✓, `training-time.spec` 5 tests ✓.

## Fix: add missing spec for `clampBuildingLevel`

`clampBuildingLevel` normalizes castle/building levels (floor, clamp 1–10, non-finite → 1) and is used by buildings.ts, village-visuals.ts, and pixi world-types.

**Change:** new `battleforthecrown-backend/src/modules/world/level.spec.ts` — 6 tests.

**Verification:** `yarn static-check` ✓, `level.spec` 6 tests ✓.

## Backlog updates

- Removed `calculateTrainingTime` and `clampBuildingLevel` from candidates (fixed).
- Retained open-PR blocked items unchanged.

## Fix: add unit spec for `clampBuildingLevel`

`clampBuildingLevel` in `packages/shared/src/utils/level.ts` normalizes castle/building levels (floor, clamp 1–10, non-finite → 1) and is used by buildings, village-visuals, and pixi world-types. Only indirect coverage existed via villageVisualTierFromCastleLevel in Pixi.

**Change:** new `battleforthecrown-backend/src/modules/world/level.spec.ts` — 6 tests (constants, in-range, floor-not-round, clamp low/high, NaN/Infinity fallback).

**Verification:** `yarn static-check` ✓, `level.spec` 6 tests ✓.

## Fix: add unit spec for `normalizeTier`

`normalizeTier` in `packages/shared/src/world/entities.ts` maps API tier strings to WorldTier literals (exact T1–T5 plus fuzzy includes fallbacks). Consumed by `entityFromWorldDto` in pixi `world-types.ts` but had zero direct test coverage.

**Change:** new `battleforthecrown-pixi/src/api/world-types.test.ts` — 16 tests (exact literals, fuzzy strings, null/unsupported inputs).

**Verification:** `yarn static-check` ✓, `world-types.test.ts` 16 tests ✓.

## Fix: add unit spec for `calculateProductionRate`

`calculateProductionRate` in `packages/shared/src/logic/production.ts` drives village resource production previews and backend ticks but had no direct pure-logic regression test.

**Change:** new `battleforthecrown-backend/src/modules/resources/production-rate.spec.ts` covering base rate, building level scaling, strategy bonuses, and non-resource building guard.

**Verification:** `yarn static-check` ✓, `production-rate.spec` ✓.

## Fix: add unit spec for `getStrategyBonusValue`

`getStrategyBonusValue` in `packages/shared/src/village/strategy.ts` resolves per-bonus numeric overrides for village strategies on combat/production paths but lacked direct unit coverage.

**Change:** new `battleforthecrown-backend/src/modules/strategy/strategy-bonus.spec.ts` covering strategy overrides, BALANCED defaults, and unknown bonus keys.

**Verification:** `yarn static-check` ✓, `strategy-bonus.spec` ✓.
