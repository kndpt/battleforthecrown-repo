# maint-debt — 2026-06-14

## Fix: remove duplicate `isFoggedEntity` from pixi/api/world-types.ts

`isFoggedEntity` type guard was defined locally in `battleforthecrown-pixi/src/api/world-types.ts:21` as an exact duplicate of `packages/shared/src/world/entities.ts:47`. The shared version was already exported from `@battleforthecrown/shared/world`.

**Change:** replaced local definition with import + re-export from shared. 1 file changed.

**Verification:** `yarn static-check` ✓, pixi 404 tests ✓.

## Backlog updates

- Removed `isFoggedEntity duplicate` from candidates (fixed).
- Added `pixi console.error stubs` and `pixi formatTime variants` as new candidates.
- Retained blocked items: formatDuration dup (product call needed), VillageCanvas (too broad), WorldMapScene (#89), training-time (#99-101), production (#102).
