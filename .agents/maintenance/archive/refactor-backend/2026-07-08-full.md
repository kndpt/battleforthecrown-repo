# refactor-backend — 2026-07-08 full report

Base : `main@86ef6f5`. Branche : `claude/relaxed-wozniak-asn0h1`.

## Phase 1 — Audit

Focus : duplication resource-credit / clamp warehouse (dimensions dup, shared-formula extraction, N+1/tx). Backlog OPEN existant repris tel quel (state file).

### Findings (nouveaux ce run)

| ID   | cat          | file:line                                                                                   | note                                                                                             | Sev  | Effort |
|------|--------------|---------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|------|--------|
| RC1  | dup          | retention.service.ts:169 / onboarding.service.ts:223 / barbarian-runtime.service.ts:121     | `min(current + delta, maxPerType)` par ressource recopié 3× (reward/regen credit)                | Med  | S      |
| RC2  | test gap     | packages/shared/src/resources/storage.ts                                                    | Le clamp capped-credit n'avait aucun home unique ni test direct                                  | Low  | S      |
| RC3  | looks-bad    | return.worker.ts:246-257                                                                     | Clamp headroom `max(0, cap - current)` — sémantique distincte (préserve over-cap), NON fusionnable | —    | —      |

### Résolu ce run → **RC1 + RC2**

Thème : **single-home warehouse-capped resource credit**.

- Nouveau helper pur `creditResourcesCapped(current, delta, maxPerType): ResourceAmounts` dans `packages/shared/src/resources/storage.ts` (à côté de `getWarehouseStorageLimit`). Nouveau type `ResourceAmounts` dans `resources/types.ts`.
- Chaque ressource → `min(current + delta, cap)` (cap unique wood/stone/iron partagé).
- 4 callsites migrés byte-for-byte :
  - `retention.service` (claim daily card reward)
  - `onboarding.service` (completion reward + initial village reward)
  - `barbarian-runtime.service` (regen credit, delta uniforme)
  - Le 2e site onboarding (`ensureForInitialVillage`, `ONBOARDING_INITIAL_REWARD`) manqué au 1er passage, ajouté sur review CodeRabbit (même pattern, mêmes garanties).
- +5 unit tests (`storage.spec.ts`) : add-under-cap, clamp per-resource indépendant, delta uniforme (barbarian), pull-down over-cap (sémantique historique), zero-delta no-op.
- Aucun changement de comportement.

### Looks bad but fine

- `return.worker.ts:246-257` (loot credit) : clamp contre le **headroom restant** `min(delta, max(0, cap - current))` puis add sans re-clamp → préserve un stock déjà au-dessus du cap (production uncapped). `creditResourcesCapped` le tirerait vers le bas → sémantique différente + la valeur `returnedResources` est réutilisée (report/porters). Laisser hors scope, documenté sur le helper.
- `combat.worker.ts` (2044L) / `combat.service.ts` (1602L) : gros, couverts par smokes, split = L risqué (W1/B1 backlog).
- `extraction-lifecycle.service.ts` (804L, 4 handlers) : neuf (run 091), split candidat quand stabilisé (G2 backlog).

## Phase 2 — Validation

- `yarn static-check` ✅ (tsc back + pixi + eslint)
- `test:backend` 572/572 ✅
- `test:pixi` 963/963 ✅ (dont `storage.spec` +5 nouveaux — les specs shared tournent sous vitest pixi)
- Smokes `daily-retention` + `onboarding` + `barbarians` + `production-tick` 13/13 ✅ (exercent les 3 sites touchés)

## Docs

Aucun changement nécessaire — refactor interne (extraction formule pure partagée), pas d'impact API/gameplay/convention/data-model. JSDoc sur le helper documente la sémantique historique (over-cap pull-down) et l'exclusion return.worker.
