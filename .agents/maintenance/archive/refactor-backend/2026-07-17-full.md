# refactor-backend — 2026-07-17 — W1b : caravan + reinforcement arrival extraction

## Thème

Suite directe de W1a (scout → `scout-arrival.service.ts`, 2026-07-16). Fin de la décomposition
du god-worker `combat.worker.ts` : extraction des deux derniers handlers d'arrivée non-combat
(`handleCaravanArrival` + `handleReinforcementArrival`/`bounceReinforcementFromCaptureWindow`)
vers des collaborateurs dédiés, calqués sur le pattern `ScoutArrivalService` /
`ExtractionLifecycleService`. Le worker ne route plus que par `kind` pour les arrivées non-combat.

## Changements

- **Nouveau** `combat/caravan-arrival.service.ts` — `CaravanArrivalService.handleArrival(tx, expedition): Promise<ReturnJobToSchedule>`.
  Déps : `ResourcesService` (`calculateCurrentResources`) + `OutboxPublisher` (`resourcesChanged`).
  Crédit stock warehouse-capped, overflow `lost`, `CaravanReport` ARRIVED + inbox, flip RETURNING,
  retourne le return job au worker (scheduling post-commit inchangé).
- **Nouveau** `combat/reinforcement-arrival.service.ts` — `ReinforcementArrivalService.handleArrival(tx, expedition): Promise<void>`
  + privé `bounceReinforcementFromCaptureWindow`. **Zéro dép injectée** (uniquement `tx` + helpers purs).
  Stationnement `Garrison` / retour home / bounce fenêtre de capture OPEN + `ReinforcementReport` + inbox.
- **Nouveau** `combat/interfaces/return-job.interface.ts` — type `ReturnJobToSchedule` partagé worker ↔ caravan
  (était local au worker).
- `combat.worker.ts` : dispatch `CARAVAN`/`REINFORCE` délègue aux services ; 3 méthodes privées supprimées
  (−408 L, 1760 → 1352). **Dép `ResourcesService` retirée du constructeur** (seul usage était caravan ;
  `OutboxPublisher` conservée, encore utilisée par `applyLootToDefender`). Imports scope-only supprimés :
  `caravan.utils` (parseCaravanResources/subtractCaravanResources/caravanPortersFor),
  `getWarehouseStorageLimit`, `PendingConquestStatus`, `creditUnitsToInventory`, `combat-report.utils`
  (dedupedRecipientUserIds/loadReportVillageSnapshot).
- `combat.module.ts` : `CaravanArrivalService` + `ReinforcementArrivalService` providers.
- `docs/architecture/backend-modules.md` : file-tree (2 collaborateurs) + flow étape 2 (délégation caravan/reinforce) + étape 6 (recall → ReinforcementArrivalService).

## Invariants préservés

- `tx` toujours fourni par le worker → même transaction `Serializable`, zéro changement de
  comportement / ordre d'écriture / events. Return job caravan planifié post-commit comme avant.
- Aucune migration, aucun changement de balance, aucune surface API/WS modifiée.

## Acceptance & QA

- `yarn static-check` ✅
- Backend unit : 610/610 ✅
- Smokes (boot Nest → DI `CaravanArrivalService` + `ReinforcementArrivalService` validée) :
  caravan + cross-player-reinforcement + reinforcements + combat-reports-inbox + recall-en-route → **16/16** ✅
  (les deux chemins extraits exercés end-to-end : crédit caravane + rapport, stationnement/retour/bounce renfort, inbox).
- Docs : `backend-modules.md` mis à jour.

## Restant sur le worker (1352 L)

God-service `handleCombatResolution` (combat inline via stratégie) reste — c'est le cœur combat,
pas un candidat d'extraction « arrival handler ». Décomposition arrival-handlers **terminée** (scout+caravan+reinforce extraits).
