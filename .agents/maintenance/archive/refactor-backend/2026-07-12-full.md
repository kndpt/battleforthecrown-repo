# refactor-backend — 2026-07-12

## Thème : RK-CFG — colocate rankings config resolver + Glory signals

### Contexte
State OPEN item **L3** : `rankings.service.ts:26` importait `resolveRankingsConfig`
(fonction pure) depuis `rankings-cycle.service.ts` → smell de layering
service→service. La constante `GLORY_SIGNALS` (paire ordonnée ASSAULT/RAMPART)
vivait aussi dans `rankings-cycle.service.ts`, module-private.

### Changement
Nouveau `battleforthecrown-backend/src/modules/rankings/rankings-config.utils.ts` :
- `GLORY_SIGNALS: RankingSignal[]` (paire ordonnée assault→rampart, ordre leaderboard).
- `resolveRankingsConfig(config): { reset, snapshotEntries }` (pure, safeParse
  `WorldConfigSchema` → fallback `DEFAULT_WORLD_RANKINGS_CONFIG`).

Câblage :
- `rankings-cycle.service.ts` : suppression des 2 défs locales, import depuis utils.
  (`CycleResetConfig` reste importé de `@shared/rankings` — encore utilisé L166/L221.)
- `rankings.service.ts` : source d'import `./rankings-cycle.service` → `./rankings-config.utils`.
- Nouveau `rankings-config.utils.spec.ts` : 3 cas (ordre GLORY_SIGNALS ; résolution
  config valide via `DEFAULT_WORLD_CONFIG` override ; fallback defaults sur null/garbage).

Move behavior-identique. Aucun changement API/gameplay/migration/balance.

### Détail vérif config
`WorldConfigSchema` = `z.strictObject` avec `tempo`/`combat`/`barbarianSeeding`/
`playerVillagePlacement`/`fogOfWar` **sans `.default()`** → un config partiel
`{rankings:{...}}` **échoue** le parse et retombe sur defaults. Le test success
utilise donc `{...DEFAULT_WORLD_CONFIG, rankings:{...}}` (config complet valide).

### Acceptance & QA
- `yarn static-check` ✅ (tsc + eslint back+pixi, 73s).
- `yarn workspace battleforthecrown-backend test` ✅ 598/598 (61 suites ; +3 vs baseline 595).
- Smokes : `yarn test:smoke` → 151/152 ; le seul échec = `combat-attack.smoke` joinWorld
  helper, **flaky sous parallélisme** (passe seul : 4/4). Sans rapport avec rankings.
- Docs : aucun changement nécessaire, raison : refactor interne pur, aucun impact
  API/gameplay/convention/data-model.

### Autres candidats audit (inchangés, cf. state OPEN)
R4 (fractional carry, migration), W1 (combat.worker split L), B1 (combat.service spec gap),
G2/SU1/TE1/E1/U1/L2/C1/K2/Z1/H2/X1 — non retenus ce run (hors thème cohérent ou low-ROI).
