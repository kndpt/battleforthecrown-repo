# refactor-backend — run 2026-06-13

## Thème : D5 — Extraction du garrison-merge loop (combat.worker.ts)

### Contexte

`buildBarbarianDefender` (L1142) et `buildPlayerDefender` (L1206) dans `combat.worker.ts` contenaient exactement le même loop 20-lignes pour merger les garnisons dans le tableau `participants` et accumuler `totalUnits`.

### Changements

| Fichier | Action |
|---|---|
| `src/modules/combat/garrison-merge.utils.ts` | Nouveau — type `GarrisonWithStrategy`, fonction `mergeGarrisonsIntoParticipants` |
| `src/modules/combat/garrison-merge.utils.spec.ts` | Nouveau — 5 tests unitaires (empty, new origin, merge existing, strategy, multi-origin) |
| `src/modules/combat/combat.worker.ts` | Import + remplacement des 2 boucles identiques par l'appel helper |

### Acceptance & QA

- `yarn static-check` ✅
- `yarn test:backend` → 359 tests, 35 suites ✅
- PR `maint/refactor-backend/garrison-merge`

### Audit Phase 1 — Findings (20+)

| ID | Cat | Fichier:ligne | Note | Sev | Effort |
|----|-----|--------------|------|-----|--------|
| B3 | Perf | event-outbox.service.ts | N+1 getUserIdByVillage | Med | M | → PR #91 ouverte |
| R4 | Logic | crowns.service.ts:264 | fractional carry — needs migration | High | L | skip |
| R3 | Layering | retention.service.ts | class + 14 util exports | Med | M |
| D4 | Dup | combat.service.ts | expedition.create ×5 — champs communs | Med | M |
| **D5** | **Dup** | **combat.worker.ts L1142+1206** | **garrison loop ×2 — TRAITÉ** | **Med** | **S** |
| G1 | Perf | combat.service.ts:986 | sequential getKingdomPowerValue — intentionnel tx | Med | — |
| F1 | Dup | combat.worker.ts | garrison.findMany include identique ×2 — cosmétique | Low | S |
| N3 | Logic | world-entities-query.service.ts:101 | radius clamp manquant | Low | S |
| C6 | Logic | resources.service.ts:50 | recursive getResources | Low | S |
| V3 | Logic | combat.service.ts:480 | caravan deduct order | Low | S |
| A1 | Logic | auth.service.ts:36 | redundant displayName pre-check | Low | S |
| T1 | Test | buildBarbarianDefender/buildPlayerDefender | pas de test isolé — couverts par smokes | Med | S |

### Looks bad but fine

- **G1** : loop séquentiel sur `getKingdomPowerValue` dans `snapshotDefenderKingdomPowers`. En transaction Prisma serializable, la parallélisation risque des deadlocks. Intentionnel.
- **D4** : `expedition.create ×5` — chaque variant (ATTACK/SCOUT/REINFORCE/CARAVAN/RECALL) a assez de champs différents pour qu'une factory unifiée soit plus complexe que le code actuel. Pas de vraie valeur.
