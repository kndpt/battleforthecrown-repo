# refactor-backend — 2026-06-21

Theme: **hoist `buildShieldState` pure builder into `packages/shared/src/pvp/shield.ts`; replace 3 inline reconstructions of `NewbieShieldState` in backend**.

## Phase 1 — Audit

Backend volume: 22 574 ts LoC; top files = `combat.worker.ts` (1776), `combat.service.ts` (1313), `conquest.service.ts` (654), `retention.service.ts` (504), `village-strategy.service.ts` (458), `onboarding.service.ts` (420), `world-entities-query.service.ts` (331), `power.service.ts` (290), `crowns.service.ts` (287).

Last 12 commits ont déjà absorbé : unification scout helper (#160), training queue (#155), defeat carousel (#163), newbie shield (#164), Outbox teardown await (#154), strategy bonus table (#144), buildings.findByType (#142), WOOD/STONE/IRON projection (#153).

### Findings

| ID  | Cat        | File:line                                           | Note                                                                                              | Sev  | Effort |
|-----|------------|-----------------------------------------------------|---------------------------------------------------------------------------------------------------|------|--------|
| S1  | dup        | world.service.ts:178-205, world-entities-query.service.ts:267-285, newbie-shield.service.ts:33-48 | trois sites construisent `NewbieShieldState` inline depuis `isShieldActive` + `shieldEndsAt`. Pas de pure builder dans shared. | Med  | S      |
| W1  | god        | combat.worker.ts (1776 L)                           | 4 kinds cohabitent (ATTACK/SCOUT/REINFORCE/CARAVAN) + Conquest side-effects. Split L effort, hors scope semaine. | High | L      |
| W2c | dup        | combat.service.ts:84-262 + 265-350                  | `initiate{Attack,Scout,Reinforce}` partagent loadOwned→verify→timing→create→event→schedule mais payloads kind-specific divergent. Template helper non-trivial. | Med  | M      |
| U1  | n+1        | combat.worker.ts:1489-1498, 1743-1759, return.worker.ts:326-340 | boucle `inboxEntry.create/upsert` ×N par recipient (max 2). `createMany skipDuplicates` possible mais ROI bas. | Low  | S      |
| U3  | dup        | world-entities-query.service.ts:137-201, 204-315    | `fetchBarbarianVillages` et `fetchPlayerVillages` partagent `bounds` clause + capture-window mapping (lignes 187-198 et 301-310). | Low  | S      |
| N3  | bug-edge   | world-entities-query.service.ts:113-116             | `getVillagesInRadius` ne clamp pas `maxX`/`maxY` à 499 (alors que `getEntitiesInRadius` le fait). Asymétrique. | Low  | S      |
| N4  | naming     | world-entities-query.service.ts:117-127             | `getVillagesInRadius`/`getAllVillages` ne renvoient pas `BARBARIAN_VILLAGE`/`PLAYER_VILLAGE` (raw rows). API surface confuse vs `getEntitiesInRadius`. | Low  | M      |
| D2  | dup        | upgrade-building.use-case.ts:40-56, recruit-troops.use-case.ts:69-88, recruit-noble.use-case.ts:53-83 | `Promise.all([village, stock, population, buildings/active, …])` répété ×3. Forme assez divergente (5/6/7 lignes). | Low  | M      |
| R4  | invariant  | crowns.service.ts:261-286 (`accumulateCrowns`)      | `production = Math.floor(rate * hours)` perd la fraction <1/h chaque tick. Migration `lastUpdateTs += production/rate` requise. | High | M      |
| C1  | smell      | resources.service.ts:46-56                          | `getResources` re-appelle récursivement après `updateProduction` plutôt que refetch stock + projection rates. Cosmétique (récursion bornée 1). | Low  | S      |
| A1  | smell      | auth.service.ts:36-42                               | Pre-check `displayName.findFirst` + P2002 catch en parallèle. Pre-check est case-insensitive (DB peut être case-sensitive), donc *pas* redondant — laissé tel quel. | Low  | —      |
| L1  | layering   | crowns.service.ts:165-199 (`recalculateOnBuildingChange`) | service métier reçoit `villageId` brut, looke village pour extraire userId+worldId. Caller `construction.worker.ts:190` détient déjà `villageId` mais pas userId/worldId. OK. | —    | —      |
| L2  | layering   | strategy/village-strategy.service.ts:389-457 (`getStrategyRecommendations`) | Strings UI françaises hard-codées dans le service. Devrait vivre dans shared/village (sans dépendance Nest). | Low  | S      |
| G1  | n+1        | combat.service.ts:1008-1015 (`snapshotDefenderKingdomPowers`) | `for of userIds` → `getKingdomPowerValue` séquentiel. Intentionnel (snapshot tx, parall. risque deadlock). State file marquait `intentionnel tx`. | —    | —      |
| Z1  | config     | world/join-world.use-case.ts:188                    | `process.env[key]` lecture dynamique. `ConfigService` ferait mieux. | Low  | S      |
| F1  | dup        | combat.worker.ts:1130-1170                          | 2× `garrison.findMany` avec include similaire. Cosmétique. | Low  | S      |
| T1  | tx-shape   | retention.service.ts:401-416 (`expireStaleCards`)   | `$transaction` qui n'enveloppe qu'**un** `updateMany`. La tx est inutile (single statement). Cosmétique. | Low  | S      |
| T2  | tx-shape   | retention.service.ts:186-209 (`recordOutboxEvent`)  | `$transaction` autour de `dailyCardProgressEvent.create` (try P2002) + `progressOldestMatchingTask`. Le second peut être no-op si pas de carte ouverte. Forme correcte. | —    | —      |
| W3  | worker-boot | construction.worker.ts:31-69 + 6 autres workers     | Pattern `createQueue → boss.work` répété ×8 avec try/catch boilerplate et émojis logs. Helper `registerQueueWorker(boss, queueName, handler, opts?)` simplifierait. Risque pg-boss subtil → hors scope. | Med  | L      |
| W4  | logs       | construction.worker.ts:33,46,57,65,153             | Émojis 🔧/🔨/🎯/✉️/✅ dans logs prod. Cohérence: les autres workers sont sobres. | Low  | S      |
| W5  | resilience | construction.worker.ts:159-197                      | 3× try/catch « Don't throw — building is already upgraded » hors-tx. Pattern: log+swallow chacun. Risque silencieux. | Med  | M      |
| O1  | spec-debt  | combat.controller.ts (341 L)                        | Largest controller. La plupart des routes délèguent au service mais quelques helpers de mapping inline. Acceptable. | —    | —      |
| O2  | api-shape  | combat.service.ts:1066-1124 (`getOpenConquests`)   | DTO mapping dans le service. Convention OK (Nest service exporte DTO). | —    | —      |
| O3  | encoding   | combat/codecs.ts (parseUnitMap, encodeCombatLoot)  | Helpers OK, bien centralisés. | —    | —      |
| B1  | tests      | combat.service.ts                                   | Aucun spec direct (smokes uniquement). Pour un service de 1313L, peu de cover unit pure-logic. | Med  | L      |
| B2  | tests      | retention.service.ts                                | Couvert via retention.service.spec.ts + smoke. OK. | —    | —      |
| E1  | event-utils | event/event.utils.ts + outbox-publisher.service.ts | `createOutboxEvent` raw vs `OutboxPublisher.<kind>` typé. Coexistence — 100+ usages directs de `createOutboxEvent` non migrés. Sweep progressif déjà engagé. | Low  | L      |
| H1  | health     | (boot)                                              | `OutboxWorker` `setInterval` (90L) — bien commenté, in-flight guard correct depuis #154. | —    | —      |
| H2  | logging    | production.worker.ts:76-87 + crown-production.worker.ts:76-92 | Boucle `for of villages` + try/catch per item. OK pour <10k villages, sinon batch. | Low  | M      |
| P1  | perf       | world.service.ts:32-49 (`getWorlds`)                | `groupBy worldMembership` séparé de `world.findMany`. Idem `getUserMemberships`. 2 round-trips au lieu d'un `_count` include. Cosmétique. | Low  | S      |
| P2  | perf       | combat.service.ts:1126-1203 (`getOpenExpeditions`) | 3 queries (attackerVillages → expeditions → targets). `findMany` de villages = bonne approche (déjà dédupliqué). OK. | —    | —      |
| N5  | naming     | world-entities-query.service.ts:330                 | `byCoord` non exporté, OK. Mais le suffixe `Schema` dans `barbarianVillageDataSchema`/`playerVillageDataSchema` mélange validation runtime + type — pourrait être nommé `…ContractSchema`. Cosmétique. | Low  | —      |
| K1  | concurrency | retention.service.ts:212-241 (`ensureDailyCard`)  | Pattern try `dailyCard.create` → catch P2002 → return. Race-safe. Pourrait être `upsert` (déjà utilisé dans `ensureDailyCardInTransaction`). Asymétrie volontaire ? | Low  | S      |
| K2  | constants  | retention.service.ts:37 `DAILY_CARD_LIMIT = 1`     | Magic 1, jamais utilisé pour limiter — seulement exposé via DTO. Trace de spec passée. | Low  | S      |
| K3  | constants  | crowns.service.ts:8 `MS_PER_HOUR`                  | OK importé depuis shared. | —    | —      |
| V1  | comment-rot | combat.service.ts:170,256 « 8b. Break attacker's newbie shield if active (PvP only) » | Numérotation « 1. 2. 3. 3b. 4. 5. 6. 7. 8. 8b. 9. » dans `initiateAttack` est lisible mais inflate la doc inline. | Low  | — |
| V2  | comments   | retention.service.ts:294-301                       | `if (currentDayKey !== eventDayKey) ensure currentDayKey` — comment manquant. Subtil: backfill du jour en cours quand event arrive après minuit Paris. | Low  | S      |

#### Looks bad but fine

- `combat.worker.ts` 1776L : god file mais la complexité est inhérente au domaine combat (4 kinds + conquest + intel + glory + reports + inbox + outbox). Le split par kind reste préférable (W1) mais demande un run dédié.
- `auth.service.ts:36` pre-check displayName (A1) : pré-check **case-insensitive** sert quand la DB constraint est case-sensitive ; pas redondant.
- `crowns.service.ts:151` asymétrie `createEvent` : volontaire, documentée dans le JSDoc + `realtime.md`.
- `combat.service.ts:1008` `snapshotDefenderKingdomPowers` séquentiel : intentionnel pour tx safety (parallèle risque deadlock).
- `outbox.worker.ts:42` `setInterval` : déjà guardé contre overlap et shutdown race (run #154).
- `production.worker.ts:76` `for of villages` séquentiel : assumé scale <10k villages, pas un goulot actuel.
- `combat.service.ts:170` `// 8b.` numbering inflated : aide les sub-runs (newbie-shield, ratio, etc.) à trouver leur place dans la séquence.

## Phase 2 — Refactor

### Theme

Pure shared helper `buildShieldState({ joinedAt, brokenAt, newbieShieldHours, now })` → `NewbieShieldState` dans `packages/shared/src/pvp/shield.ts`. Remplace 3 reconstructions inline backend.

### Changements

**shared**

- `packages/shared/src/pvp/shield.ts` : ajoute `buildShieldState` (compose `shieldEndsAt` + `isShieldActive`, formate ISO strings). Exporte `NewbieShieldState` re-exporté depuis pvp pour évacuer le couplage avec `world/dtos` ailleurs (laissé pour compat).
- `packages/shared/src/pvp/shield.spec.ts` : 4 cas (active+null brokenAt, expired naturally, brokenAt set, exact boundary).

**backend**

- `world.service.ts:178-205` : `getUserMemberships` → utilise `buildShieldState` (1 appel au lieu de 3 lectures shieldEndsAt+isShieldActive).
- `world-entities-query.service.ts:259-285` : `fetchPlayerVillages` → `buildShieldState` puis `state.active ? state : undefined`.
- `newbie-shield.service.ts:36-46` : `getMembershipShieldState` → délègue à `buildShieldState`.

### Tests

- `yarn workspace @battleforthecrown/shared build` ✅
- `yarn workspace @battleforthecrown/shared test src/pvp/shield.spec.ts` → 4 nouveaux passants
- `yarn static-check` ✅
- `yarn test:backend` (unit) ✅
- Smoke `pvp-newbie-shield` ✅ (validation que les 3 sites observent le même état)

### Risques

- Aucun changement de comportement attendu : pure refactor (même calcul, même output shape). Smokes existants captent toute régression.
- `buildShieldState` doit retourner exactement les mêmes ISO strings que les inline. Vérifié : `shieldEndsAt(...).toISOString()` partout, `brokenAt ? brokenAt.toISOString() : null` partout.

### Pas inclus (hors thème)

- W1 (combat.worker split par kind), R4 (crowns fractional carry migration), W2c (template combat.initiate*), W3 (worker-boot helper), L2 (strategy UI strings), C1 (resources recursive), N3 (radius clamp).
