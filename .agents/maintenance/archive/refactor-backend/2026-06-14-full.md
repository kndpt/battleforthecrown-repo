# refactor-backend — run 2026-06-14

## Thème : R3 — Extraction des utils retention hors du fichier service

### Contexte

`retention.service.ts` (666 lignes) contenait 8 fonctions exportées pures + 3 helpers privés
**après** la fermeture de la classe `@Injectable`, violant la convention NestJS.
Ces fonctions n'étaient importées par personne en dehors du module retention.

### Changements

| Fichier | Action |
|---|---|
| `src/modules/retention/retention.utils.ts` | Nouveau — 8 exports purs + 3 helpers privés |
| `src/modules/retention/retention.service.ts` | 666 → 504 lignes ; imports nettoyés (drop BattleResolvedPayload, BuildingCompletedPayload, UnitTrainedPayload, compareBarbarianTier) ; import depuis ./retention.utils |
| `src/modules/retention/retention.service.spec.ts` | Import redirigé vers ./retention.utils |
| `test/daily-retention.smoke.spec.ts` | Import redirigé vers ./retention.utils |

### Acceptance & QA

- `yarn static-check` ✅
- `yarn workspace battleforthecrown-backend test` → 370 tests, 37 suites ✅
- Hook pre-push complet (backend + pixi) ✅
- PR #109 `maint/refactor-backend/retention-utils`

### Audit Phase 1 — Findings nouveaux (run 2026-06-14)

| ID | Cat | Fichier:ligne | Note | Sev | Effort |
|----|-----|--------------|------|-----|--------|
| **R3** | **Layering** | **retention.service.ts** | **class + 8 util exports — TRAITÉ** | **Med** | **M** |
| E1 | Perf | event-outbox.service.ts:600 | `getUserIdByVillage` appelé 21× ; ~1 requête DB par appel dans la boucle `dispatchPendingEvents` — pas de batching | Med | M |
| O2 | Layering | onboarding.service.ts:191 | `getOnboardingProjection` fn pure exportée depuis service (même pattern R3) | Low | S |

### Looks bad but fine

- **B3 (event-outbox N+1)** : déjà identifié en 2026-06-13, PR #91 close. Le pattern `getUserIdByVillage` séquentiel est lié à la structure actuelle de `dispatchPendingEvents`. E1 est le même finding avec comptage exact (21 appels potentiels).
