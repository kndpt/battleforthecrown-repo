# refactor-backend — run 2026-07-06

Branche : `claude/relaxed-wozniak-jxii3i` (env designé) · base `origin/main` @ a7c2ff4.

## Phase 1 — Audit

`find src -name '*.ts' | wc -l | sort` + churn 3 mois + scan dimensions (dup, layering, type debt, errors, N+1, config, test gaps).

État global : **sain**. Peu de type debt (`as unknown as Prisma.InputJsonValue` seuls, tous légitimes codecs JSON), **zéro catch avalé**, DTOs Zod, Outbox typé (`createOutboxEvent<K>` enforce payload↔kind compile-time).

### Findings (fmt `ID | cat | file:line | note | Sev | Effort`)

- `CD1 | dup | loot.manager.ts:75 + extraction-lifecycle.service.ts:773 | helper carryCapacity-sum dupliqué byte-for-byte (getUnitStats × quantity sur UnitMap) | Med | S` ← **THÈME retenu**
- `W1 | god | combat.worker.ts (2022L) | 4 kinds cohabitent — split par kind | High | L`
- `B1 | test | combat.service.ts (1602L) | pas de spec unit direct (smokes only) | Med | M`
- `G2 | god | extraction-lifecycle.service.ts (816L) | 8 handlers (arrival/completion/return/interception + resolve×2) dans un service — split candidat quand stabilisé | Med | M`
- `E1 | outbox | 16 fichiers, 60 callsites createOutboxEvent | migration OutboxPublisher — REQUALIFIÉ low-value : createOutboxEvent déjà typé (générique K), OutboxPublisher n'ajoute de valeur que resourcesChanged (fetch stock+rates). Reste = wrapper nommé. Churn > bénéfice | Low | L`
- `R4 | correctness | crowns.service.ts:261 | fractional carry — needs migration (`lastUpdateTs += production/rate`) | High | M`
- `L3 | layering | rankings.service.ts:26 | import resolveRankingsConfig cross-service | Low | S`
- `U1 | perf | combat.worker.ts:1478+, return.worker.ts:326 | inbox.create loop ×N → createMany skipDuplicates (ROI bas, ≤2 recipients) | Low | S`
- `L2 | dead | village-strategy.service.ts:382 | getStrategyRecommendations strings UI FR hard-codées, endpoint sans consumer front | Low | M`
- `C1 | style | resources.service.ts:44 | getResources récursif (récursion bornée 1, cosmétique) | Low | S`
- `K2 | dead | retention.service.ts:42 | DAILY_CARD_LIMIT=1 magic jamais utilisé pour limiter | Low | S`
- `Z1 | config | join-world.use-case.ts:188 | process.env[key] dynamique — ConfigService mieux | Low | S`
- `H2 | perf | production.worker.ts:63, crown-production.worker.ts:76 | for-of villages séquentiel (OK <10k) | Low | M`
- `X1 | dead | village.controller.ts:132 /village/strategy/recommendations | endpoint sans consumer front | Low | S`

### Looks bad but fine

- `combat-resolution.ts` `sumUnits`/`sumAttackPower`/`sumDefensePower` : module-private, **non dupliqués** ailleurs → laisser local.
- `combat.utils.ts:sumPopulationCost` déjà single-home partagé combat↔extraction → précédent exact suivi par CD1.
- `as unknown as Prisma.InputJsonValue` (loot.codec, payload.codec, rankings-cycle) : frontière JSON obligatoire, encodeurs typés en amont.
- `power.service.ts:216` unit-sum : pondéré par power stat, sémantique distincte de carry — pas un doublon.
- OutboxPublisher partiel (5 kinds) : couvre les kinds à payload dérivé (stock/rates). Migration exhaustive = churn (E1 requalifié).

## Phase 2 — Refactor (thème CD1)

Home unique `sumCarryCapacity(units: UnitMap)` dans `combat/combat.utils.ts`, à côté de `sumPopulationCost` (même rationale anti-cycle worker↔service, doc alignée).

- `loot.manager.ts` : `calculateTotalCarryCapacity` privé supprimé → import `sumCarryCapacity`. Imports `getUnitStats`/`typedEntries`/`UnitMap` devenus inutiles retirés.
- `extraction-lifecycle.service.ts` : `sumCarryCapacity` privé supprimé → import partagé. Import `getUnitStats`/`typedEntries` retirés (`UnitMap` conservé, largement utilisé).
- `combat.utils.spec.ts` : +4 cas (`sumCarryCapacity` : somme multi-type, ignore carry 0, map vide, quantité 0).

Comportement strictement inchangé (helper byte-for-byte identique, garde `quantity` ajoutée = no-op car qty≥0). Pas de migration, pas de balance, API/events inchangés.

Diff : 4 fichiers, +52 −37 (source nette −14L).

## Acceptance & QA

- `yarn static-check` : green (tsc back+pixi, eslint back+pixi).
- `yarn workspace battleforthecrown-backend test combat.utils loot.manager` : **35 passed**.
- Smokes ciblés `extraction caravan combat-attack` (préflight template `_smoke` OK) : **20 passed** — 3 chemins loot (interception extraction, caravane, attaque village).
- Docs : aucun changement nécessaire — refacto interne, pas d'impact API/gameplay/convention.
</content>
</invoke>
