# refactor-backend — 2026-07-09

## Thème retenu

**Single-home combat population release.** Le pattern « libérer la population des
unités mortes » = `sumPopulationCost(losses)` → guard `> 0` →
`tx.population.update({ where: { villageId }, data: { used: { decrement } } })`
était dupliqué inline 3× dans `combat.worker.ts` et re-homé en méthode privée dans
`extraction-lifecycle.service.ts` (4 appels). Extrait en fonction pure-orchestration
`releasePopulationForLosses(tx, villageId, losses)` dans un nouveau fichier
`modules/combat/population-release.ts`.

## Audit (Phase 1)

Dimensions balayées : layering, god services, Prisma/tx dup, Outbox, errors, type
debt, config, test gaps, naming. Backlog durable dans `refactor-backend.state.md`
(R4, W1, B1, G2, …). Findings frais notables :

| ID  | cat        | file:line                                   | note                                                                 | Sev | Eff |
|-----|------------|---------------------------------------------|----------------------------------------------------------------------|-----|-----|
| P5  | dup/tx     | combat.worker.ts:271,1288,1318 + extraction:678 | population-release pattern dupliqué 4 homes → **RETENU**          | Med | S   |
| P6  | purity     | combat.utils.ts                             | pur (math) — ne pas y mettre de tx-write (respecté : nouveau fichier)| —   | —   |
| P7  | dup        | extraction:512-590 vs 599-671               | resolveInterceptionVictory/Defeat partagent le double release + updates expedition ; dedup partielle possible mais branches divergentes (candidat futur, ROI moyen) | Low | M |

### Looks bad but fine

- `return.worker.ts:278` (`used.decrement: porters`) et `conquest.service.ts:611/657`
  (`noblePopulation`, `remainingPopulation`) **ne sont PAS** des releases de pertes
  d'unités de combat — sémantiques distinctes (retour de porteurs, coût noble à la
  conquête). Hors scope, laissés tels quels.
- `combat.worker.ts:1288` gardait un guard `!isBarbarian` supplémentaire : villages
  barbares n'ont pas de ligne `population`. Guard déplacé au callsite, helper neutre.

## Refactor (Phase 2)

- **Nouveau** `modules/combat/population-release.ts` — `releasePopulationForLosses`.
  Importe `sumPopulationCost` (combat.utils) + `PrismaClientOrTx`. Pas de cycle.
- `combat.worker.ts` — 3 sites migrés, import `sumPopulationCost` retiré (plus utilisé),
  guard barbarian conservé au callsite (site local reinforcement).
- `extraction-lifecycle.service.ts` — méthode privée supprimée, 4 appels →
  fonction libre, import `sumPopulationCost` retiré.
- Sémantique préservée byte-for-byte (no-op-on-zero interne = ancien guard `> 0`).

## Acceptance & QA

- `yarn static-check` ✅ (tsc back+pixi + eslint --quiet).
- `yarn test:backend` ✅ 577 unit / 59 suites.
- Smokes ciblés ✅ 17 (`combat-attack` = release attaquant, `reinforcements` =
  participants renfort, `extraction` = interception 2 côtés) — couvrent les 4 sites migrés.
- Docs : aucun changement nécessaire (dedup structurelle interne, pas d'API/gameplay/convention).

## PR

`maint(refactor-backend): single-home combat population release` — branche
`claude/relaxed-wozniak-ou9ndg` (branche imposée harness).
