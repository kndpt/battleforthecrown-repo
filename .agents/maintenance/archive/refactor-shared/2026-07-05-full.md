# refactor-shared — 2026-07-05

## Contexte

Candidat #1 de `refactor-shared.state.md` (run 2026-06-22) : `village/definitions.ts` (476 L) mêlait types, constantes de coûts, et helpers.

PR #186 (candidat rankings du run précédent) était déjà mergée — pas de conflit de scope.

## Changements

| Fichier | Contenu |
|---------|---------|
| `village/building-types.ts` | `BUILDING_TYPES`, `BuildingType`, `BuildingLevelDefinition`, `BuildingDefinition` |
| `village/building-costs.ts` | `BUILDING_DEFINITIONS` (table de données pure, coûts/temps par bâtiment/niveau) |
| `village/building-helpers.ts` | `BUILDING_UNLOCK_REQUIREMENTS`, `getBuildingUnlockRequirement`, `getBuildingDefinition`, `getBuildingLevelValues`, `getBuildingMaxLevel`, `isBuildingEnabled`, `findBuildingByType`, `getBuildingLevel` |
| `village/definitions.ts` | Barrel re-exportant les trois modules |

## API publique

`village/buildings.ts` → `export * from './definitions'` inchangé. `definitions.spec.ts` inchangé (import depuis `./definitions`). Aucun changement visible pour backend/pixi.

## Résultats

- `yarn static-check` : green
- `yarn workspace battleforthecrown-pixi test` : 932 passed (129 fichiers)
- `yarn workspace battleforthecrown-backend test` : 560 passed (59 suites)
