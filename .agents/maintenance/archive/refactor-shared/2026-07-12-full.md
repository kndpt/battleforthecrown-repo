# refactor-shared — 2026-07-12

## Contexte

PR #259 (candidat #1 du run 2026-07-05, split `village/definitions.ts`) déjà mergée — pas de conflit de scope. Nouveau run : candidat #1 restant dans `refactor-shared.state.md`, test gap `logic/` (`calculateTravelTime`, `calculateBuildingCost`, `calculateProductionRate`, zéro couverture). Re-vérifié à l'audit : ces trois formules sont désormais consommées par backend **et** Pixi (pas backend-only comme noté au run précédent) — valeur relevée de moyenne à réelle.

## Changements

| Fichier | Contenu |
|---------|---------|
| `logic/travel-time.spec.ts` | `calculateTravelTime` (distance/vitesse/armySpeedBonus), `calculateDistance`, `findSlowestUnitSpeed`, `getCaravanResourceCapacity` |
| `logic/building-cost.spec.ts` | `calculateBuildingCost` (castle bonus, speedMultiplier, clamp 1s, strategy no-op documenté) |
| `logic/production.spec.ts` | `calculateProductionRate` (strategy productionBonus × trait naturel, compoundé) |

Aucun changement de comportement — tests purs ajoutés uniquement. Runners via `vitest` pixi (`include: packages/shared/src/**/*.spec.ts`), pas de runner shared dédié.

## Résultats

- `yarn static-check` : green
- `yarn test:pixi` : 1053 passed (136 fichiers, +3 fichiers/+38 tests)
- `yarn test:backend` : 598 passed (61 suites)

## Note découverte pendant l'audit

`calculateBuildingCost` : `buildingCostReduction` et `constructionSpeedBonus` existent dans `StrategyBonus` mais **aucune des 4 stratégies actuelles ne les override** (toutes restent à leur défaut 1.0). Testé explicitement (le test échouera si une stratégie future ajoute un override sans mise à jour). Pas un bug — juste documenté.
