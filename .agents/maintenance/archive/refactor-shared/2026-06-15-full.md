# refactor-shared — 2026-06-15

Deux runs le même jour (dead-surface puis orphaned specs).

---

## Run 1 — dead-surface

### Audit

#### Bloquant — `resources/affordability.ts` mort
4 fonctions + types (`canAffordCost`, `costIncludesPopulation`, `getResourceCostsOnly`,
`getPopulationCostOnly`, `PlayerResources`, `AffordabilityCheck`, `Cost`).
`PlayerResources` avait `gold?`/`food?` (ressources inexistantes dans le jeu).
Grep : 0 import en backend ou pixi.

#### Majeur — Dead exports `village/buildings.ts`
- `BuildingLevelCost` : alias `@deprecated` de `BuildingLevelDefinition` — 0 usage
- `getBuildingLevelCost()` : wrapper de `getBuildingLevelValues` — 0 usage

#### Majeur — Alias `CrownsConfig` (`crowns/index.ts`)
Alias legacy de `CrownsSettings` — 0 usage dans backend/pixi.

#### Mineur — Test gap formules pures
`logic/travel-time`, `logic/building-cost`, `logic/training-time` : 0 spec dans shared.
Le backend couvre indirectement `buildings.ts` mais pas les formules de `logic/`.
→ Non traité ce run (hors scope).

### Refactor appliqué

Thème : `dead-surface`

- Suppression `packages/shared/src/resources/affordability.ts`
- Retrait `export * from './affordability'` dans `resources/index.ts`
- Suppression `BuildingLevelCost` + `getBuildingLevelCost` dans `village/buildings.ts`
- Suppression `CrownsConfig` dans `crowns/index.ts`

64 lignes supprimées, 4 fichiers.

### QA

- `yarn static-check` : vert
- `yarn test:backend` : 420/420
- `yarn test:pixi` : 439/439

### Commit

`refactor(shared): remove dead surface — affordability module, deprecated aliases`
Branch : `claude/bold-hopper-lrqqkb`

---

## Run 2 — orphaned shared specs → pixi vitest

### Audit Phase 1

**Périmètre** : 60 fichiers TS, ~4 412 LOC, 16 modules.

**Points clés trouvés :**

| Dimension | Statut |
|---|---|
| Imports workspace interdits | ✓ CLEAN |
| Duplication cross-workspace | ✓ CLEAN |
| Couverture tests | 2 spec files vitest, aucune exécutée par un runner |
| Fichiers sur-dimensionnés | village/buildings.ts 537L, events/types.ts 375L, events/schemas.ts 301L |
| Exports morts | ✓ CLEAN (post run 1) |
| Incohérences schema/type | combat/index.ts : exports type redondants ; auth/schemas.ts : displayName optional vs required type |

**Problème prioritaire choisi** : les 2 fichiers `.spec.ts` de `packages/shared/src/` (`rankings/index.spec.ts`, `auth/display-name.spec.ts`) importent de `vitest` mais ne sont dans aucun `include` de runner. Identique à `travel-time.spec.ts` supprimé par maint-debt PR #106. La différence : ces 2 specs sont utiles (formules rankings, validation displayName).

### Phase 2 — Refactor appliqué

**Fichier modifié** : `battleforthecrown-pixi/vitest.config.ts`

Ajout de :
```typescript
include: [
  'src/**/*.{test,spec}.{ts,tsx}',
  '../packages/shared/src/**/*.spec.ts',
],
```

**Résultat** :
- Test files : 74 → 76 (+2 shared)
- Tests : 439 → 450 (+11 assertions)
- `yarn static-check` : ✓

**Doc mise à jour** : `battleforthecrown-pixi/AGENTS.md` — couverture vitest mention shared specs.

### Findings restants (hors scope ce run)

- `combat/index.ts` : `export type { ... } from './dtos'` (lignes 16-29) redondant avec `export * from './dtos'` (ligne 31). Candidat prochain run.
- `village/buildings.ts` (537L) : monolithique, candidat split (definitions.ts, speed-bonuses.ts, vision.ts).
- `events/types.ts` + `events/schemas.ts` : couplage manuel 30 payloads, risque drift. Ajouter `satisfies Record<EventKind, z.ZodType>` compile-time guard.
- `auth/schemas.ts` : `displayName` optionnel en input schema vs required en type output. Transform compense mais mismatch confus.

### Commit

`maint(shared/tests): wire orphaned shared vitest specs into pixi test runner`
Branch : `claude/dreamy-einstein-j2w0sn` — PR #123
