# refactor-shared — 2026-06-17 — combat/index.ts cleanup

## Thème
Supprimer le bloc `export type {...}` redondant et l'alias `CombatConfig` dans `packages/shared/src/combat/index.ts`.

## Audit Phase 1

| Candidat | Statut |
|---|---|
| combat/index.ts — redundant export type block + CombatConfig alias | ✅ **ce run** |
| events/schemas.ts — satisfies guard | ✅ **déjà en place** (ligne 297 `as const satisfies Record<EventKind, z.ZodType>`) |
| village/buildings.ts split (524L) | ⏳ prochain run |
| auth/schemas.ts displayName optional/required | ⏳ pas de bug réel — mismatch conceptuel masqué par annotation `: AuthSessionResponse` |
| test gap logic/ | ⏳ backlog |

## Modifications

**`packages/shared/src/combat/index.ts`** — 18 suppressions :
- Lignes 16-29 : `export type { TargetKind, ExpeditionStatus, ... }` → couverts par `export * from './dtos'`
- Lignes 13-14 : `// Legacy alias` + `export type CombatConfig = CombatRules` → zéro consumer (backend a son propre `CombatConfig` dans `combat-context.interface.ts`)

## Acceptance & QA

- `yarn test:backend` : 458/458 ✅
- `yarn test:pixi` : 457/457 ✅
- `yarn static-check` : erreurs pré-existantes run-054 (narrativeTargetVillageId, originKind) non liées à ce run

## PR

`maint(refactor-shared): remove redundant combat index exports`
