# refactor-pixi — 2026-07-06 — extraction WS invalidation

## Theme

Fix missing/incomplete WS invalidation for the 3 extraction event handlers (added in run 091) + 1 silent cancel-training mutation in UnitCard.

## Audit findings (full)

### NEW findings

| ID | Cat | Where | Description | Sev |
|----|-----|-------|-------------|-----|
| WS-10 | ws-invalidation | ws-bindings.ts:1192 | `extraction.started` was a no-op; site ring stale until 30s poll | Med |
| WS-11 | ws-invalidation | ws-bindings.ts:1140 | `extraction.returned` missing resources/activeExpeditions/army/population invalidation | Med |
| WS-12 | ws-invalidation | ws-bindings.ts:1129 | `extraction.attacked` missing resources/army/population when `interrupted` | Med |
| D30 | silent-mutation | UnitCard.tsx:147 | cancel.mutate() with no onError — no user feedback on failure | Med |
| C-04 | god-component | WorldMapScreen.tsx:515L | 48 hooks, 9 useState, 6 useEffect — not previously tracked | High |
| C-05 | god-component | GameHeader.tsx:470L | 35 hooks, duplicates VillageView profile/village-selector | High |
| TYPE-04 | type-debt | SegmentedControl.tsx:20 | onChange typed as string; 8 callers cast back to specific union | Med |
| TYPE-05 | type-debt | BuildingDto.type, ArmyUnitDto.type | ~20 `as BuildingType`/`as UnitType` casts across codebase | Med |
| TYPE-06 | type-debt | api/client.ts:168,174 | `undefined as T` silently returns undefined to typed callers | Med |
| DRY-02 | duplication | scoutReportView, CaravanLaunchModal, naturalTraitInfo, combatReportView | Triplicated RESOURCE_LABELS + RESOURCE_ICONS maps | Med |
| DRY-03 | duplication | 8+ files | Proliferation of independent `Intl.NumberFormat('fr-FR')` instances | Med |
| DRY-04 | duplication | PowerBottomSheet:20, MultiVillageBottomSheet:163 | Duplicate tierFromPower with divergent tier-6 threshold | Med |

### Previously tracked — status updates

| ID | Status |
|----|--------|
| C-01 | ArmyScreen 688L (+7), 29 hooks, 7 useState — unchanged severity |
| C-03 | VillageView 600L (+27), 51 hooks, 10 useState — grew, still critical |
| P-01 | WorldMapScene 1073L (+54) — grew, still critical |
| D28-29 | NOW STALE: both exports have consumers (ResourceDisplay, VillageViewSections) |
| TYPE-02 | RESOLVED: scoutReportView refactored, pattern removed |
| D09 | RESOLVED: onSuccess in useTrainUnitsMutation (F01 from prior run) |
| S-01 | resources/crowns dual store verified clean — intentional interpolation pattern |
| S-02 | All store consumers use granular selectors — verified |
| SESS-01 | mapMarkersStore.clear() in resetGameSessionStores — verified |

### Looks bad but fine

| Where | Why |
|-------|-----|
| ws-bindings.ts 1233L | Flat registry, 38 events × ~20L each; no nesting, each handler self-contained |
| SpecializedBuildingDetailModal.tsx 636L | Pure presentational, 0 hooks, 5 building sub-components |
| VillageViewSections.tsx 531L | Pure presentational, 0 hooks, markup-only |
| stores/ui.ts 131L | 3 domains share same lifecycle (session-scoped transient). STR-02 is cosmetic |

## Fixes applied

1. **ws-bindings.ts** — `applyExtractionStarted`: new handler replacing no-op; invalidates worldEntities + activeExpeditions + armyInventory + population.
2. **ws-bindings.ts** — `applyExtractionAttacked`: added conditional (interrupted) invalidation of resources + activeExpeditions + armyInventory + population.
3. **ws-bindings.ts** — `applyExtractionReturned`: added invalidation of resources + activeExpeditions + armyInventory + population.
4. **UnitCard.tsx** — cancel training mutation: added pushToast onError with ApiError message extraction.
5. **ws-bindings.test.ts** — 4 new test cases: extraction.started, extraction.attacked (non-interrupted + interrupted), extraction.returned.

## Acceptance & QA

- `yarn workspace battleforthecrown-pixi test` — 936 passed (129 test files)
- `yarn static-check` — clean (tsc + eslint)
- 4 new tests cover all 3 extraction handlers + interrupted branch
