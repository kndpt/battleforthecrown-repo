# refactor-pixi — 2026-07-11 — DRY-02 resource metadata consolidation

## Theme

DRY-02 (Crit): 13 files duplicated resource labels/icons/paths that `RESOURCE_CONFIG` in `lib/resourceConfig.ts` already provides.

## Audit findings

### Refreshed existing items

| ID | Status | Note |
|----|--------|------|
| DRY-02 | **FIX (7/13)** | 7 production files consolidated → RESOURCE_CONFIG. 6 design-system files left (crowns icon path intentionally different: `/assets/casual-icons/crown.png` vs `/assets/crown.png`). See below. |
| DRY-04 | OPEN High | 3x tierFromPower confirmed (PowerBottomSheet, MultiVillageBottomSheet, PlayerProfileSheet). tier 5 vs tier 6 divergence. |
| D05 | OPEN High | 4 compact number formatters confirmed (formatResourceAmount, formatHeaderCompactAmount, formatCompactNumber in resourceConfig.ts + frShort in meta.ts). |

### New findings

| ID | Sev | Where | Note |
|----|-----|-------|------|
| C-10 | Med | ReportDetailModal.tsx:496L | 4 near-identical report-type sections (Scout, Combat, Caravan, Reinforcement). Consolidation candidate. |

### Looks bad but fine

| Where | Why |
|-------|-----|
| naturalTraitInfo.ts RESOURCE_LABELS (3 entries) | Uses uppercase ResourceType from shared package; key type mismatch makes consolidation ugly for minimal gain. |
| Design-system files with `/assets/casual-icons/crown.png` | Intentionally different icon from RESOURCE_CONFIG's `/assets/crown.png` — decorative vs currency crown. |

## Changes (7 files)

1. **CaravanLaunchModal.tsx** — removed `RESOURCE_LABELS` + `RESOURCE_ICONS`; replaced with `RESOURCE_CONFIG[key].assetPath` / `.nameCapitalized`
2. **scoutReportView.ts** — removed `RESOURCE_ICONS` + `RESOURCE_LABELS`; replaced with `RESOURCE_CONFIG[resource].assetPath` / `.nameCapitalized`
3. **combatReportView.ts** — removed `RESOURCE_ICONS`; replaced with `RESOURCE_CONFIG[resource].assetPath`
4. **caravanReportView.ts** — removed `resourceMeta`; replaced with `RESOURCE_CONFIG[key].assetPath` / `.nameCapitalized`
5. **VillageViewSections.tsx** — `RESOURCE_BUTTONS` icon/label → `RESOURCE_CONFIG.<key>.assetPath` / `.nameCapitalized`
6. **GameHeader.tsx** — resources useMemo icon/label → `RESOURCE_CONFIG.<key>.assetPath` / `.nameCapitalized`
7. **ResourceBuildingDetailModal.tsx** — `RESOURCE_BUILDING_META` icon → `RESOURCE_CONFIG.<key>.assetPath`; label → `RESOURCE_CONFIG.<key>.name` (iron/stone/wood); `quarter.icon` → `RESOURCE_CONFIG.population.assetPath`

## Verification

- `tsc --noEmit` clean
- `yarn test:pixi` 985/985 green
- `yarn static-check` clean
