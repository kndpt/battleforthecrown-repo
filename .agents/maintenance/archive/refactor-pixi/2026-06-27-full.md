# refactor-pixi — 2026-06-27 — DRY-invalidation-helpers-round-2

## Theme

Continue DRY extraction of invalidation helpers (round 2). Fix bug in `applyNobleKilled` WS handler.

## Changes

### ws-bindings.ts (-39 lines net)

**New helper**: `invalidateVillageEconomy(ctx, villageId)` — combines `resources + population` invalidation.
Replaced in 10 handlers: `applyBuildingCompleted`, `applyUnitTrained`, `applyUnitTrainingCompleted`, `applyBattleResolved`, `applyExpeditionRecalled`, `applyCaravanSent`, `applyCaravanArrived`, `applyCaravanRecalled`, `applyCaravanReturned`, `applyVillageAttacked`.

**Bug fix**: `applyNobleKilled` was missing `invalidatePowerQueries(ctx, payload.attackerVillageId)` and `invalidateCombatReports(ctx)`. Noble death changes army power (unit lost) and generates a combat report — without these, HUD showed stale data.

### queries.ts (-14 lines net)

**New helper**: `invalidateArmyMutationQueries(qc, villageId)` — combines `armyTraining + armyInventory + resources + population`.
Replaced in 3 mutations: `useTrainUnitsMutation`, `useRecruitNobleMutation`, `useCancelTrainingMutation`.

**New helper**: `invalidateBuildingMutationQueries(qc, villageId)` — combines `queue + buildings + resources + population`.
Replaced in 2 mutations: `useUpgradeBuildingMutation`, `useCancelConstructionMutation`.

### ws-bindings.test.ts (+10 lines)

Extended `applyNobleKilled` test to seed and assert invalidation of: `villagePower`, `kingdomPower`, `rankingsSummary`, `combatReports`, `scoutReports`.

## Stats

- 3 files, 52 insertions, 105 deletions (net -53 lines)
- 820/820 tests green
- Pixi tsc ✅, pixi eslint ✅

## Acceptance & QA

- [x] Unit tests pass
- [x] Type-check clean
- [x] Lint clean
- [ ] Manual QA: noble death → power/report refresh in HUD (Kelvin)

## PR

#210 — `refactor(pixi/api): DRY invalidation helpers + fix applyNobleKilled`
