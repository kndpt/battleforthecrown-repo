# refactor-pixi — 2026-07-09

## Theme: DRY-02 — Consolidate resource display constants

### Audit summary

Dimensions scanned: DRY (resource constants, formatters, tierFromPower), type debt (as casts, undefined as T, missing onError), god components, test gaps.

New exports added to `lib/resourceConfig.ts`:
- `PrimaryResourceKey` type (`'wood' | 'stone' | 'iron'`)
- `PRIMARY_RESOURCE_KEYS` array
- `RESOURCE_ICON_PATHS` — asset paths derived from `RESOURCE_CONFIG`
- `RESOURCE_DISPLAY_LABELS` — French names derived from `RESOURCE_CONFIG`
- `RESOURCE_META` — combined `{icon, label}` for report/caravan views
- `RESOURCE_BAR_FILL` — progress bar gradient CSS classes

### Files changed (10 production files)

| File | Change |
|------|--------|
| `lib/resourceConfig.ts` | Added 6 derived exports |
| `features/combat/scoutReportView.ts` | Removed local RESOURCE_ICONS + RESOURCE_LABELS → import canonical |
| `features/combat/combatReportView.ts` | Removed local RESOURCE_ICONS → import canonical |
| `features/world/CaravanLaunchModal.tsx` | Removed local RESOURCE_ICONS + RESOURCE_LABELS → import canonical |
| `features/combat/caravanReportView.ts` | Removed local resourceMeta → import RESOURCE_META |
| `features/game/VillageViewSections.tsx` | RESOURCE_BUTTONS derived from canonical maps instead of hardcoded |
| `features/layout/GameHeader.tsx` | Resource bar array derived from canonical maps instead of hardcoded |
| `features/village/ResourceBuildingDetailModal.tsx` | Icon paths → RESOURCE_CONFIG[type].assetPath |
| `features/game/naturalTraitInfo.ts` | RESOURCE_LABELS values → RESOURCE_CONFIG[type].nameCapitalized |
| `features/onboarding/onboardingViewModel.ts` | Reward preview icons → RESOURCE_ICON_PATHS |
| `features/retention/DailyRetentionWidget.tsx` | rewardPills icons → RESOURCE_ICON_PATHS |

### Validation

- `yarn static-check` ✅ (tsc + eslint back+pixi)
- `yarn workspace battleforthecrown-pixi test` → 981/981 passed ✅
- No backend src/ changes → smokes not required

### OPEN items (carried forward, updated)

See `refactor-pixi.state.md` for current open items.

### Design-system preview files not touched

~7 design-system preview/showcase files also duplicate resource icons/labels. These are dev-only visual prototypes — intentionally left untouched to keep the PR scope bounded to production code.
