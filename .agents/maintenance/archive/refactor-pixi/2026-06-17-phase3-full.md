# refactor-pixi — 2026-06-17 — Phase 3: WS invalidation consistency

## Theme

Continuation of the WS invalidation consistency work started in the previous run (PR #135). This run addressed the remaining gaps where WS handlers invalidated fewer cache keys than their REST mutation counterparts, causing stale data in multi-tab or reconnect scenarios.

## Audit dimensions

| Dimension | Status |
|-----------|--------|
| Server-authoritative violations | Clean — no violations in prod code |
| TanStack Query keys/staleTime | 4 queries missing staleTime (Low, deferred) |
| Zustand stores | Well-designed (28-72 LOC), no god-stores |
| WS invalidations | **4 gaps found and fixed** |
| Pixi scenes | WorldMapScene 759 LOC (god-scene, deferred). Lifecycle clean. |
| God components | VillageView 530L, ArmyScreen 637L (deferred) |
| Type debt | Zero — clean codebase |
| Test gaps | Several feature dirs with zero tests (deferred) |

## Findings fixed

| ID | Handler | Missing Invalidation(s) | REST Reference |
|----|---------|------------------------|----------------|
| F01 | applyBattleSent | `activeExpeditions(villageId)` | useInitiateAttackMutation |
| F02 | applyScoutSent | `activeExpeditions(villageId)` | useInitiateScoutMutation |
| F03 | applyExpeditionRecalled | `resources(villageId)`, `population(villageId)`, `activeExpeditions(villageId)` | useRecallExpeditionMutation |
| F04 | applyCaravanSent | `activeExpeditions(villageId)` | useInitiateCaravanMutation |

## Bonus fix

pre-push hook: replaced bash-isms (`set -euo pipefail`, `< <(...)` process substitution, `local` keyword) with POSIX sh equivalents. Husky v9 runner explicitly invokes `sh -e`, ignoring the bash shebang.

## Test changes

- Updated 3 existing test descriptions and assertions (applyBattleSent, applyScoutSent, applyExpeditionRecalled)
- Added 1 new test block (applyCaravanSent)
- Test count: 456 → 458

## Acceptance

- `yarn workspace battleforthecrown-pixi test`: 458/458 pass
- `yarn static-check`: clean (backend + pixi)
- `yarn test:backend`: 458/458 pass
- Pre-push hook: runs successfully in sh
- PR #138 created, CI pending
