# refactor-pixi 2026-06-14 — useGarrisonSelection

## Cible
C3 — ArmyScreen garrison derivations inline, extractable hook

## Critère de fin
`useGarrisonSelection` extrait, ArmyScreen réduit, tests verts, static-check OK.

## Acceptance & QA
- static-check clean (backend + pixi tsc + eslint)
- 412 tests pixi (404 existants + 8 nouveaux sur le hook)
- PR `claude/affectionate-ptolemy-nk2dbd` sha `d9ecd9a`

## Synthèse
3 useState couplés (selectedGarrisonVillageId/Direction/TroopId) + 5 dérivés inline dans ArmyScreen → `useGarrisonSelection(garrisonLines, troops)`. ArmyScreen perd 35 lignes ; hook couvert par 8 tests unitaires. `formatGarrisonOriginsSubtitle` déplacé dans le hook. `garrison` (query) renommé `garrisonQuery` pour éviter conflit.
