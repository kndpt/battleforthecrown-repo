# refactor-pixi — 2026-06-28 — WS invalidation gaps

## Theme: Missing cache invalidations in WS event handlers

Audit identified 3 WS handlers with missing power/garrison cache invalidation and 1 redundant polling config.

## Findings (Phase 1)

Full audit across 4 dimensions: queries+WS, god-components, Pixi scenes+stores, type-debt+test-gaps.

### P1

| ID | Cat | File:line | Description |
|----|-----|-----------|-------------|
| F01 | server-auth | queries.ts:870-889 | useTrainUnitsMutation: optimistic update with hardcoded timePerUnitMs=60_000 |
| F02 | god-component | VillageView.tsx:96 | 26 hooks, 8 useState for overlays, 40+ props drilled |
| F03 | god-component | ArmyScreen.tsx:299 | 681L, mixed barracks/garrison/onboarding |

### P2 (addressed this run)

| ID | Cat | File:line | Description |
|----|-----|-----------|-------------|
| F05 | invalidation | ws-bindings.ts:603-614 | applyCaravanArrived missing power invalidation for target |
| F06 | invalidation | ws-bindings.ts:667-679 | applyGarrisonAdded missing power invalidation for host |
| F07 | invalidation | ws-bindings.ts:797-844 | applyVillageConquered missing garrison invalidation |
| F10 | consistency | queries.ts:740-756 | rankingsSummaryQuery: staleTime=30s AND refetchInterval=30s redundant |

### Looks bad but fine

- DesignSystemPreview.tsx (3361L): storybook catalog, size justified.
- applyResourcesChanged dual store+query: intentional pattern (real-time HUD + server-authoritative).
- WorldMapScene (930L): iso terrain+fog tightly coupled, split expensive for low gain.
- SpecializedBuildingDetailModal (27 props): pure presentation, acceptable.

## Changes (Phase 2)

### ws-bindings.ts
- `applyCaravanArrived`: added `invalidatePowerQueries(ctx, payload.targetVillageId)` — resource delivery changes village/kingdom power.
- `applyGarrisonAdded`: added `invalidatePowerQueries(ctx, payload.villageId)` — garrison troops affect power.
- `applyVillageConquered`: added `queryKeys.garrison(payload.villageId)` invalidation — previous garrison data stale after conquest.

### queries.ts
- `rankingsSummaryQuery`: refetchInterval 30_000 → 120_000. WS-driven invalidation (applyRankingsChanged + invalidatePowerQueries) covers normal path; poll is now self-heal fallback only.

### ws-bindings.test.ts
- applyCaravanArrived test: added seedPowerQueries + expectPowerQueriesInvalidated assertion.
- applyGarrisonAdded test: added setCurrentWorldSession + seedPowerQueries + expectPowerQueriesInvalidated assertion.
- applyVillageConquered test: added garrison seed + invalidation assertion.

## Acceptance

- 825 pixi tests green
- tsc pixi clean
- Backend tsc errors are pre-existing Prisma migration gaps (unrelated)
