# refactor-pixi — 2026-07-02 — ws-bindings test coverage + session state hardening

## Theme

WS-03: 5 WS event handlers were untested (applyAttackIncoming, applyRankingsChanged, applyRankingsCycleClosed, applyVillageRemoved, applyIntelUpdated). SESS-01: mapMarkersStore (added in run 085) was missing from resetGameSessionStores(), leaking markers across user sessions.

## Audit Findings

| ID | Cat | File:line | Description | Sev | Effort |
|----|-----|-----------|-------------|-----|--------|
| F01 | optimistic-ui | queries.ts:886 | useTrainUnitsMutation: hardcoded timePerUnitMs=60_000 | Critical | S |
| F02 | optimistic-ui | queries.ts:1538 | useUpgradeBuildingMutation: hardcoded endTime 60_000 | Critical | S |
| C-01 | god-component | ArmyScreen.tsx:681L | 11 hooks, 7 useState, no test | High | L |
| C-03 | god-component | VillageView.tsx:573L | 20 hooks, 9 useState, 7 boolean flags, no test | High | L |
| P-01 | monolith | WorldMapScene.ts:1019L | 11+ responsibilities, split into 7 managers feasible | High | XL |
| WS-01 | god-function | ws-bindings.ts:827 | applyVillageConquered 51 lines, 5 responsibilities | High | M |
| S-01 | dual-source | resources.ts, crowns.ts | Zustand + TQ cache (intentional for interpolation) | High | L |
| TEST-01 | test-gap | ArmyScreen, VillageView, AttackDetailModal | 3/4 major screens ZERO tests | High | XL |
| SESS-01 | session-leak | mapMarkersStore.ts | Not in resetGameSessionStores() | Med | S |
| WS-03 | test-gap | ws-bindings.test.ts | 5 handlers untested | Med | M |
| Q-09 | monolith | queries.ts:~2035L | 50+ hooks in single file (PR #233 pending) | Med | L |
| STR-02 | anti-pattern | stores/ui.ts:62-63 | Module-level mutable seqs, not reset on logout | Med | S |
| S-03 | mixed-concerns | ui.ts:131L | Toasts + modals + defeats mixed | Med | M |
| TEST-02 | test-gap | MapMarkerSheet.tsx | New component without tests | Med | M |

### Looks bad but fine

- C-04: SpecializedBuildingDetailModal 636L — purely presentational, each building type factored to sub-component. Well-designed.
- S-02: Store selectors — all 56 Zustand consumer calls use granular selectors, no full-state subscriptions.
- P-02/P-03: Entity/viewport listener leaks — cleanup via viewport.removeAllListeners() in exit() is correct (implicitly paired).
- WS-02: worldId null in invalidate helpers — harmless (invalidates non-matching key), auth flow guarantees worldId before WS events.
- staleTime values consistent (2s combat, 5s resources, 30s config, 5m immutable).
- No `as` type bypasses remain in queries.ts (cleaned in prior runs).
- All optimistic mutations have proper onError rollback.

## Changes

### ws-bindings.test.ts
- Added 5 describe blocks for previously untested handlers:
  - `applyAttackIncoming`: verifies incomingAttacks query invalidation for target village
  - `applyRankingsChanged`: verifies rankingsSummary query invalidation
  - `applyRankingsCycleClosed`: verifies rankingsSummary + rankingCycles + rankingTitles invalidation (conditional on auth userId)
  - `applyVillageRemoved`: verifies worldEntities invalidation + worldMapStore.removeEntity
  - `applyIntelUpdated`: verifies villageIntel query invalidation keyed by worldId+villageId
- Added 5 imports for the new handlers

### features/world/mapMarkersStore.ts
- Added `clear()` action that resets markers to [] and selectedMarkerId to null

### stores/session.ts
- Added import for useMapMarkersStore
- Added useMapMarkersStore.getState().clear() call in resetGameSessionStores()

### stores/session.test.ts
- Added import for useMapMarkersStore
- Added mapMarkersStore seeding in seedAllSessionStores()
- Added assertions for markers and selectedMarkerId after reset

## Validation

- `yarn static-check` ✅ (tsc + eslint, backend + pixi)
- `yarn test:pixi` → 886/886 ✅ (+5 new tests)
- Pre-push hook pending (will run on push)
