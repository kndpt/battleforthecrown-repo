# refactor-pixi 2026-06-22 — full audit report

## Theme: query layer type-safety & cache hygiene

PR #182 merged. 2 files changed, 9 insertions, 6 deletions.

## Audit scope

407 source files in battleforthecrown-pixi/src scanned across 8 dimensions:
- Pixi scene patterns (factory, reconciliation, cleanup)
- Server-authoritative compliance
- WS event coverage (32/32)
- Type debt (as any, @ts-ignore, unknown)
- TanStack Query keys & staleTime
- Zustand store patterns
- Test coverage (105 files, 676 tests)
- God components & monoliths

## Health summary

| Dimension | Verdict |
|---|---|
| Pixi scene patterns | EXCELLENT — factory + Map reconciliation + proper exit cleanup |
| Server-authoritative | PASSED — no violations in scene/store layer |
| WS event coverage | PASSED — 32/32 events handled, timeout cleanup correct |
| Type debt | GOOD — 1 `as any` (comment), 0 @ts-ignore |
| TanStack Query | GOOD — centralized queryKeys (1 exception fixed), staleTime on all queries |
| Zustand stores | OK — interpolation architecture intentional (resources/crowns fast-path) |
| Test coverage | GOOD — 105 files/676 tests, audit false-positive on buildingLockState/constructionProgress (both tested) |
| God components | WARNING — VillageView 565L, ArmyScreen 681L, queries.ts 1713L |

## Fixes applied

1. **Q1** — `useMyVillagesQuery` missing staleTime (was 0, default → refetched on every render). Fixed: `staleTime: 30_000`.
2. **Q2** — `useVillageIntelQuery` used inline `["intel", worldId, villageId]` bypassing the `queryKeys` registry. Fixed: added `queryKeys.villageIntel()` factory, used in both `queries.ts` and `ws-bindings.ts`.
3. **T1** — `CombatLootDto.artifacts` and `items` typed `unknown[]` but never read in frontend. Fixed: removed.
4. **T2** — `CombatReportDto.details.occupationDefense` typed `unknown` but shape known from backend. Fixed: `{ attackerVillageId: string; defenderUserId?: string }`.
5. **T3** — `VillagePowerDto.breakdown` typed `Record<string, unknown>` but never read. Fixed: removed.

## Looks bad but fine

- **Resources/Crowns Zustand stores** (F9): architecture intentionnelle — Zustand = fast-path interpolation 1Hz tick, TanStack = authoritative REST. WS ne fait pas invalidateQueries exprès.
- **Expedition phase scheduling**: timeouts visuels d'animation, pas d'état autoritatif.
- **Test gaps on buildingLockState/constructionProgress**: audit agent false positive — both have .test.ts files with full branch coverage.

## Verification

- `yarn static-check` — tsc + eslint: PASS
- `yarn test:backend` — 489 tests: PASS
- `yarn test:pixi` — 676 tests: PASS
- Pre-push hook: PASS
- CodeRabbit review: no actionable comments
- CI: PASS (static check & unit tests)
