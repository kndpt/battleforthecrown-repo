# refactor-pixi — 2026-07-04 — session-ctx-dry-invalidation-helpers

## Theme

Extract `SessionCtx` to eliminate redundant store reads in ws-bindings.ts invalidation helpers.

## Audit findings

| ID | Cat | File:Line | Description | Sev | Effort |
|----|-----|-----------|-------------|-----|--------|
| DRY-01 | DRY | ws-bindings.ts:735-825 | 9 invalidation helpers each independently read useAuthStore/useGameStore (12+6 redundant reads per complex event) | High | Low |
| WS-01 | god-fn | ws-bindings.ts:827-877 | applyVillageConquered 51L, 5 responsibilities | High | Med |

### Looks bad but fine

| ID | File | Reason |
|----|------|--------|
| WS-01 | ws-bindings.ts:827 | 51 lines but sequential, clear, each responsibility 2-5 lines. Reclassified after session reads consolidated. |
| STR-02 | stores/ui.ts:62-63 | toastSeq/victoryModalSeq module-level — functionally harmless (IDs just need uniqueness, not sequential-from-0) |
| S-01 | resources.ts, crowns.ts | Dual source (Zustand + TQ) intentional for interpolation — documented in AGENTS.md |

## Refactoring applied

**File:** `battleforthecrown-pixi/src/api/ws-bindings.ts` (+124 -104)

1. Added `interface SessionCtx { userId: string | null; worldId: string | null; }`
2. Added `function resolveSessionCtx(): SessionCtx` — single store read point
3. Updated 9 private invalidation helpers to accept `session: SessionCtx` parameter:
   - `invalidateCombatReports`, `invalidateReinforcementReports`, `invalidateCaravanReports`
   - `invalidateOpenConquests`, `invalidateOpenExpeditions`
   - `invalidatePowerQueries`, `invalidateRetentionSummary`, `invalidateOnboardingSummary`
   - `invalidateVillageVisualQueries`
4. Updated 25 event handlers to call `resolveSessionCtx()` once and pass it down
5. Converted last remaining direct read in `applyRankingsCycleClosed`

Result: store reads per event reduced from O(n_helpers_called) to exactly 1.

## Verification

- `yarn static-check` green (tsc + eslint, backend + pixi)
- `yarn test:backend` 554/554
- `yarn workspace battleforthecrown-pixi test` 897/897
- Pre-push hook green

## PR

https://github.com/kndpt/battleforthecrown-repo/pull/249
